services:
  - type: web
    name: pinche-cocina-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python cerebro.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: TELEGRAM_TOKEN
        sync: false
